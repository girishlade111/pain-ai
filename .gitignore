# Dependency directories
node_modules/
dist/

# Rust / Tauri
src-tauri/target/
target/
# Phase 12: packaged sidecar one-dir builds (hundreds of MB) are release
# artifacts, never committed. The tracked 235KB stub keeps dev builds green.
# Canonical staging is src-tauri/binaries/engine/ (bundled as $RESOURCE/engine/);
# engine-*/ covers legacy triple stagings from earlier drafts.
src-tauri/binaries/engine/
src-tauri/binaries/engine-*/

# Python / Sidecar
sidecar/.venv/
sidecar/dist/
__pycache__/
*.py[cod]
.venv/
venv/

# Environment and Secret Files
.env*
*key*
*secret*
*.pem

# AI Models
*.onnx

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Logs & Cache
*.log
*.sqlite3
*.db
*.db-journal
*.db-wal
state.db
*.pdb
