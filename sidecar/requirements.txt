fastapi==0.133.1
uvicorn==0.41.0
sse-starlette==3.4.11
# Phase 10: document-generation backends for the Hermes productivity skills
# (productivity/{pdf,docx,xlsx,powerpoint}). Pinned to the versions verified
# on Python 3.11 (sidecar venv) and 3.14 (tests/terminal fallback).
python-docx==1.2.0
pypdf==6.19.0
reportlab==5.0.1
pdfplumber==0.11.10
openpyxl==3.1.5
python-pptx==1.0.2
# Phase 11: real voice engines (STT faster-whisper, TTS Piper). Verified on
# Python 3.11 (sidecar venv) and 3.14 (tests/terminal fallback). Models are
# data, not packages: whisper weights auto-download to models/stt, Piper
# voice en_US-lessac-medium to models/piper (63MB .onnx, not in git).
faster-whisper==1.2.1
piper-tts==1.8.0
